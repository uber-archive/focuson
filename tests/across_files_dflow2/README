This is a more complex test, it is spread across 3 files and a few classes

It is a mini flask app that tests

* dataflow across functions
* dataflow across files
* dataflow across classes/methods
* imports

It has its own venv you can use to run main.py to ensure it isn't broken code
